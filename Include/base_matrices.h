#ifndef base_matrices_h
#define base_matrices_h

/* This header's purpose is to call the needed headers. The classes
 * will have the following dependency:
 * base_common <- base_sparse <- base_factor <- base_dense
 */

#include "base_triplet.h"
#include "base_extra.h"

#endif
